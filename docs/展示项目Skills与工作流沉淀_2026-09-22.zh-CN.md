# 展示项目 Skills 与工作流沉淀记录

## 问题与范围

室内、展台和混合场地展示已经积累照片/点云取证、家具和开口纠错、IFC 叠加、驾驶、多语言与服务器发布经验，但部分技能参考和辅助工具只存在于本机。继续复制案例脚本，会重复发现建筑坐标轴不一致、驾驶舱世界坐标滞后、字体轮廓镂空和发布字段丢失等问题。

本次将可复用部分整理为条件性技能参考和便携工具，按用户要求同步 GitHub。未纳入本机并行工程改造、原始采集、照片、模型、案例生成器、服务器凭据或已运行站点的改动。

## 修改内容与文件

- `.agents/skills/reconstruct-indoor-scene/`：整理薄入口及既有取证、展台、精修和 BIM 参考；新增 `interactive-showcase-polish.md` 与 `delivery-verification.md`，覆盖共同建筑坐标系、驾驶舱局部视点、字体填充、完整语言状态和运行字段/发布一致性。兼容入口保留严格 authority 边界。
- `.codex/skills/reconstruct-indoor-scene/SKILL.md`、`.codex/skills/reconstruct-site-scene/SKILL.md`：按需路由到对应参考；同步全景与点云融合参考。未改变九阶段契约、验收阈值或状态写入权限。
- 两份中文 SOP 与 `docs/WORKFLOW.zh-CN.md`：建立从实际问题到相关检查的入口，区分缓存复用、局部重建、浏览器复测、真实部署与外部软件验收。
- `scripts/indoor_presentation_evidence.py` 与 `inspect_brand_asset.py`：同步已在展示流程中使用的源绑定区域观察和原始 Logo 边界检查工具，使用合成数据验证可复用行为。
- `delivery_inventory.py`：新增标准库文件清单、精确变更范围和最终目录核验工具；发现额外/丢失/过期文件，拒绝路径穿越、链接、大小写冲突和重复 JSON 键。工具只检查已准备的静态目录，不上传或读取凭据。
- `tests/test_presentation_*.py` 与 `.github/workflows/presentation-skills.yml`：加入相应合成测试及独立 CI，避免改写正在并行迁移的原有 CI 文件。

可移植性修正：未发布的新 `indoor-recon` 包命令改为条件入口，保留已提交的 `reconstruction_loop.py` 与权威技能路径；本机 showcase、案例记录和全景适配器仅作为可选参考，不能成为克隆技能后的隐式依赖。

## 实际验证

- 三个技能入口的 skill-creator `quick_validate.py`：通过。
- 25 项 pytest：通过，覆盖不同原点/轴向的合成 LAS、缓存绑定与拒绝错误复用、Logo 透明内容边界与源字节不变、文件清单顺序无关、指定变更范围、其他模型被删、过期/额外文件、非便携路径和重复键等。
- `scripts/validate_portable_repo.py`：通过；没有原始数据、机器绝对路径或密钥候选。
- 本次涉及的 Markdown 本地链接与 UTF-8/乱码候选检查：通过；Git diff 空白检查通过。
- 当前主工作区 `scripts/sync_skills.py --check`：通过。该脚本属于当地尚未发布的工程迁移，未作为本便携技能包的必需依赖。
- 已提交兼容命令 `reconstruction_loop.py --help`：通过，确认严格流程入口存在。

## 状态与边界

本次交付是技能、工作流和可复用检查工具；25 项测试不证明任意真实场景的测量精度，也不等于客户或外部 BIM 软件验收。未更改本次线上展示模型，未重新执行无关真实采集建模、部署或 IFC 导入。没有可比速度基准，因此不声明提速百分比。

GitHub 推送及 CI 的最终状态以当前任务交付消息和远端提交/PR 为准，不在提交前预写成功结论。
