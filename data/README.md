# Local data mount

Put each processed capture under this directory, or keep the capture elsewhere and pass its absolute path to `run-demo.ps1`.

Raw point clouds, camera photos, pose files, videos and SDK outputs are intentionally ignored by Git. Do not force-add customer data. A collaborator should receive source data through an approved data-transfer channel and verify its capture fingerprint locally.
